# IDEs
.vscode
.idea

# Rust/Cargo
debug/
target/
# TODO(template) Cargo.lock should be tracked in binary crates, but not in libraries
Cargo.lock

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# These are backup files generated by rustfmt
**/*.rs.bk

lcov.info

# Circom
circuits/compiled

circuits/**/*.r1cs
circuits/**/*.sym
circuits/**/*.wasm
circuits/**/*.cpp
circuits/**/*.hpp
circuits/**/*.asm
circuits/**/witness_calculator.js
circuits/**/generate_witness.js
circuits/**/*.dat
circuits/**/Makefile